{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1>Auteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ auteur.Nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ auteur.prénom }}</td>
            </tr>
            <tr>
                <th>Date_de_naissance</th>
                <td>{{ auteur.DateDeNaissance }}</td>
            </tr>
            <tr>
                <th>Date_de_création</th>
                <td>{{ auteur.DateDeCréation }}</td>
            </tr>
            <tr>
                <th>Date_de_modification</th>
                <td>{{ auteur.DateDeModification }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_auteur_index') }}">back to list</a>

    <a href="{{ path('app_auteur_edit', {'id': auteur.id}) }}">edit</a>

    {{ include('auteur/_delete_form.html.twig') }}
{% endblock %}
